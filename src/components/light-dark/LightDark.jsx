import { Switch } from '@chakra-ui/react';
import { useContext } from 'react';

export function LightDark() {
    function handleSwitch() {
        useContext(ThemeContext)
    }
   <Switch size="lg" colorScheme="red" onClick={}/>;
}
